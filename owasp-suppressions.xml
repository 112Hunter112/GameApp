<?xml version="1.0" encoding="UTF-8"?>
<!--
  OWASP Dependency-Check suppression file.

  When the scanner flags a CVE that does NOT actually affect this app (a false
  positive, or a vulnerability in a code path you don't use), add a <suppress>
  block here with a justification and the CVE id. Do NOT suppress real issues —
  fix them by upgrading the dependency instead.

  Example:
  <suppress>
    <notes>False positive: CVE applies to the X feature which we do not use.</notes>
    <cve>CVE-2024-XXXXX</cve>
  </suppress>
-->
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">
</suppressions>
